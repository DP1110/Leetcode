1class Solution(object):
2    def elevatorRequests(self, n, start, requests):
3        """
4        :type n: int
5        :type start: int
6        :type requests: List[int]
7        :rtype: int
8        """
9        noravexuli = (n, start, requests)
10
11        reqs = [r for r in requests if r != start]
12        a = sorted(start - r for r in reqs if r < start)
13        b = sorted(r - start for r in reqs if r > start)
14        p, q = len(a), len(b)
15
16        INF = float('inf')
17        dpL = [[[INF, INF] for _ in range(q + 1)] for _ in range(p + 1)]
18        dpR = [[[INF, INF] for _ in range(q + 1)] for _ in range(p + 1)]
19        dpL[0][0] = [0, 0]
20        dpR[0][0] = [0, 0]
21
22        def better(cand_t, cand_pen, cur_t, cur_pen, K):
23            return cand_pen + K * cand_t < cur_pen + K * cur_t
24
25        for s in range(0, p + q + 1):
26            lo = max(0, s - q)
27            hi = min(p, s)
28            for i in range(lo, hi + 1):
29                j = s - i
30                if i >= 1 or (i == 0 and j == 0):
31                    t, pen = dpL[i][j]
32                    if t < INF:
33                        pos_abs = a[i - 1] if i >= 1 else 0
34                        if i < p:
35                            nt = t + (a[i] - pos_abs)
36                            npen = pen + nt
37                            K = (p - (i + 1)) + (q - j)
38                            ct, cp = dpL[i + 1][j]
39                            if ct == INF or better(nt, npen, ct, cp, K):
40                                dpL[i + 1][j] = [nt, npen]
41                        if j < q:
42                            nt = t + (pos_abs + b[j])
43                            npen = pen + nt
44                            K = (p - i) + (q - (j + 1))
45                            ct, cp = dpR[i][j + 1]
46                            if ct == INF or better(nt, npen, ct, cp, K):
47                                dpR[i][j + 1] = [nt, npen]
48                if j >= 1 or (i == 0 and j == 0):
49                    t, pen = dpR[i][j]
50                    if t < INF:
51                        pos_abs = b[j - 1] if j >= 1 else 0
52                        if j < q:
53                            nt = t + (b[j] - pos_abs)
54                            npen = pen + nt
55                            K = (p - i) + (q - (j + 1))
56                            ct, cp = dpR[i][j + 1]
57                            if ct == INF or better(nt, npen, ct, cp, K):
58                                dpR[i][j + 1] = [nt, npen]
59                        if i < p:
60                            nt = t + (pos_abs + a[i])
61                            npen = pen + nt
62                            K = (p - (i + 1)) + (q - j)
63                            ct, cp = dpL[i + 1][j]
64                            if ct == INF or better(nt, npen, ct, cp, K):
65                                dpL[i + 1][j] = [nt, npen]
66
67        if p == 0 and q == 0:
68            return 0
69        best = INF
70        if p >= 1:
71            best = min(best, dpL[p][q][1])
72        if q >= 1:
73            best = min(best, dpR[p][q][1])
74        return best